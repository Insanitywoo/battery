# agent.md

## 1. 项目目标

本项目目标是形成面向不一致串联磷酸铁锂电池组的安全快充与主动均衡协同控制研究成果，最终冲击IEEE Transactions高水平期刊。

项目的总体技术基线位于：

- `整体技术方案.md`

开始任何研究、代码、数据或实验工作前，必须先阅读该文件。

## 2. 当前执行状态

- 已于2026-09-19获得按`整体技术方案.md`分步骤执行软件开发、只读数据检查、派生数据处理、训练和仿真的授权。
- 每完成一个可独立验收的小点，必须停止继续扩展并向用户汇报完成内容、验证结果、文件变化和下一小点。
- 未经用户单独明确确认，不得运行真实充放电、主动均衡或其他硬件控制。
- 遇到会修改原始数据、改变硬件状态、产生显著费用或偏离总体技术方案的操作，必须先获得确认。

## 3. 技术方案同步规则

- `整体技术方案.md`是项目唯一的总体方案基线。
- 研究目标、创新点、算法结构、数据范围、实验矩阵、投稿目标或时间安排变化时，必须同步更新该文件。
- 每次重要更新应修改版本号、更新日期并追加变更记录。
- 不得只在聊天、代码注释或临时文档中改变方案而不更新总体方案。
- 实施细节可以写入独立文档，但不得与总体方案冲突。

## 4. 研究证据边界

- 严格区分：数值仿真、硬件在环、单体实验、Pack实验。
- 降阶模型结果不得表述为真实硬件、BMS、寿命或安全收益。
- 未重新运行和核验的历史结果仅可作为路线参考，不得作为当前已验证结论。
- 所有论文指标必须能够追溯到代码版本、配置、随机种子、原始结果文件和统计脚本。
- 不得选择性报告成功案例；失败率、失效案例和最差场景必须保留。

## 5. 数据规则

- 原始数据只读，不得覆盖、重命名或原地清洗。
- 清洗、中间和特征数据写入独立派生目录，并记录来源文件、转换脚本和版本。
- 明确电流符号、单位、时间基准和缺失值处理方式。
- 同一连续轨迹不得随机拆分到训练集和测试集，避免时间泄漏。
- Pack数据必须记录单体顺序，禁止因排序导致单体身份丢失。
- `testpack.csv`实际为XLSX格式，读取时应按工作簿处理，不得误用CSV解析。
- 涉及数据公开、上传或外发前，必须确认知识产权和隐私要求。

## 6. 模型与安全规则

- 硬安全不能主要依赖奖励惩罚，必须保留独立安全过滤或投影层。
- 必须区分并记录请求动作、分配动作、安全动作和真实执行动作。
- Critic、Replay Buffer及性能分析所使用的动作定义必须明确。
- 必须报告安全层介入次数、原因、幅度及潜在越界请求率。
- 模型安全保证必须注明其参数范围、残差边界和适用条件。
- 不得宣称无条件或绝对硬件安全。
- QP、状态估计或通信失败时必须有确定性回退控制。
- 动作平滑只作为消融或工程选项，不默认视为安全保证。

## 7. 公平比较规则

不同控制器必须统一：

- 初始状态和参数样本；
- 温度和不一致场景；
- 安全边界；
- 终止条件；
- 时间步长；
- 充入能量、损耗和均衡能量核算；
- 评估案例和随机种子。

至少保留CC-CV、MCC、规则主动均衡、联合MPC、无安全层RL及完整方法等基线。若无法公平实现某基线，应明确说明，而不是使用不等价结果比较。

## 8. 可复现性与质量门槛

- 固定并记录随机种子；
- 训练、评估和绘图配置版本化；
- 关键实验使用多种子和置信区间；
- 代码变更后运行测试和静态/编译检查；
- 不手工修改最终统计结果；
- 图表必须由结果文件和脚本生成；
- 对异常高性能、零安全违规和明显优于基线的结果优先排查数据泄漏、终止条件和能量核算错误。

## 9. 硬件实验规则

- 未经用户明确批准，不执行任何真实电池充电、放电、均衡或控制命令。
- 真实平台训练禁止无保护随机探索。
- 硬件BMS和独立保护不得被软件控制器绕过。
- 新控制器部署顺序：离线回放、Monte Carlo、HIL、小电流测试、单次正常工况、扩展工况。
- 通信中断、传感器异常、QP失败或模型失配时立即回退。
- 若无法测量实际均衡电流，不得将命令值冒充执行值。
- 若只有最高/最低温度，不得声称所有单体温度均受到直接约束。

## 10. 论文写作规则

- 不声称首次进行RL主动均衡、联合快充均衡、SAC快充均衡或一般安全动作投影。
- 创新声明必须与近三年文献逐项对比。
- 摘要和结论中的每个性能数字必须能在主文、表格或补充材料中找到对应实验。
- 明确区分训练场景、测试场景、仿真结果和实物结果。
- 不用单次最优结果代替多种子统计结果。
- 论文A和论文B必须有独立贡献，避免仅将仿真论文增加实验后重复投稿。

## 11. 文件与代码操作规则

- 优先使用非破坏性操作；删除、覆盖、迁移原始数据前必须获得确认。
- 不修改与当前任务无关的文件。
- 新增目录和文件应采用清晰、稳定的英文命名；论文与总体方案可使用中文文件名。
- 临时文件、缓存、模型权重和大规模结果不得无规划堆放在项目根目录。
- 任何自动生成文件必须能够通过脚本重新生成。

## 12. 决策优先级

出现冲突时按以下优先级处理：

1. 人员与电池硬件安全；
2. 用户当前明确指令；
3. `整体技术方案.md`；
4. 公平性、可复现性和证据边界；
5. 实现速度与便利性。

